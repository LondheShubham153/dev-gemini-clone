apiVersion: v1
kind: Service
metadata:
  name: gemini-service
  namespace: gemini-namespace
  labels:
    app: gemini
spec:
  type: LoadBalancer
  selector:
    app: gemini
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30080