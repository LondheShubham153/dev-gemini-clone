version: '3.8'

services:
  gemini:
    build: .
    container_name: gemini
    ports:
      - "3000:3000"
    #env_file:
    #  - .env.local
    environment:
      # Google OAuth credentials
      - GOOGLE_ID=691564599482-9sgrhm8ef24b1jnoh1cjumdk9adolhch.apps.googleusercontent.com
      - GOOGLE_SECRET=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
      # MongoDB connection string
      - MONGODB_URI=mongodb+srv://amitabhdevops2004:R4XBQvrCzbDLCyDb@devgemini.7veo5.mongodb.net/
      # NextAuth secret
      - NEXTAUTH_SECRET=BLUvkGUOznUvS5qzirrMa7Ualgn47+BPNIeLAsYaBfc=
      # Base URL for the application
      - NEXTAUTH_URL=https://gemini.letsdeployit.com
      # Google API Key
      - NEXT_PUBLIC_API_KEY=AIzaSyDatJJCoM_WOm43Pmv2GJtBnZFlqvGMECY
    volumes:
      - /app/node_modules
    networks:
      - gemini-network
    depends_on:
      - mongodb
    restart: always

  # Add MongoDB service
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - gemini-network
    #env_file:
    #  - .env.mongodb
    environment:
      - MONGO_INITDB_ROOT_USERNAME=amitabhdevops2004
      - MONGO_INITDB_ROOT_PASSWORD=R4XBQvrCzbDLCyDb
      
volumes:
  mongodb_data:

networks:
  gemini-network:
    driver: bridge
